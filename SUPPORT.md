# Support

For usage questions, include your operating system, grat version, the relevant
sanitized `grat.config` section, command output, and steps to reproduce.

Do not include access tokens, private URLs, credentials, or complete logs that
may contain sensitive application output. For vulnerabilities, use the private
process in [SECURITY.md](SECURITY.md).
